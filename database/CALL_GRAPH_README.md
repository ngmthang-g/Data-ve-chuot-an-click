# Call graph

Static IL call edges: **12257** edges. Split into 4 JSONL chunks inside the exact full-static archive preserved under `raw/full-dump/base64-parts/`.

Fields: caller token/name, IL offset, opcode, callee token/name.
